#include <iostream>
#include <string> // Include the <string> header for string manipulation
using namespace std;

int main() {
    int num = 100;
    string temp = to_string(num);
    // cout << to_string(num); // Convert the integer to a string and print it
    cout << temp.length();
    // cout << int('a');
    cout << endl;
    // for(int i=0; i < 3; i++ ){

    // }
    return 0;
}